post:
  tags:
    - Payout
  summary: Get Available Banks
  description: | 
    The bank operation method call will return a JSON object with the all the available banks per country.
  responses:
    '201':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/Responses/Payout/banks.yaml'
          examples:
            OK: #todo mudar
              value : 
                {
                  "status": "SUCCESS"
                }     
    '400':
      description: 'invalid input, object invalid'
    '409':
      description: an existing item already exists
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/Requests/Payout/banks.yaml'
    description: Exchange body object
    required: true
#  x-code-samples:
#    - lang: Curl
#      source:
#        $ref: ../../../code_samples/CURL/exchange.curl