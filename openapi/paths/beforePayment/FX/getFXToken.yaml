post:
  tags:
    - FX
  summary: Get an FX Token
  description: |

    This is a description
    > **Note** 
    > This is a description

  operationId: getFXToken
  security:
    - integration_key: []
  responses: 
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../../components/schemas/PaymentResponse.yaml'
          examples:
            OK: #todo mudar
              value : 
                {
                  "say": "hello world"
                }           
  requestBody:
    content:
      application/json:
        schema:
          oneOf: 
            - $ref: '../../../components/schemas/RequestBody.yaml'
            - $ref: '../../../components/schemas/SimpleSchema.yaml'
    description: Payment body object
    required: true
  x-code-samples:
    - lang: Curl
      source:
        $ref: ../../../code_samples/CURL/direct/post.curl