# The following example is a transfer cancel request 
# using transfer external_reference:

curl -X POST 'https://sandbox.ebanxpay.com/ws/transfer/cancel' \
    -d 'integration_key=your_test_integration_key' \
    -d 'external_reference=PAYOUT_TRANSFER_01'